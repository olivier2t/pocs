version: "2"
name: Node.js WebApp
canonical: stack-nodejs
status: private
author: Cycloid
description: |
  Build and deploy a Node.js web application on a cloud instance.
image: |
  https://cycloid-demo-stacks-logo.s3.eu-west-1.amazonaws.com/node.svg
keywords:
  - cloud-init
  - user data
  - webapp
  - nodejs
  - nginx
  - provider:aws
technologies:
  - technology: nodejs
    version: "n/a"
  - technology: nginx
    version: "n/a"

config:
  default:
    name: 'AWS'
    description: 'Build and deploy a Node.js web application on a cloud instance'
    cloud_provider: aws
    # Pipeline and variable configuration for the default CD pipeline use case
    pipeline:
      pipeline:
        path: pipeline/pipeline.yaml
      variables:
        path: pipeline/variables.sample.yaml
        destination: ($ project $)/pipeline/($ environment $)/variables.yaml
    # Configuration of terraform (leave .sample extension to prevent terraform executing the sample file)
    terraform:
      main:
        path: 'terraform/aws/main.tf'
        destination: '($ project $)/terraform/aws/($ environment $)/main.tf'
